One liner printing an index of tuples

```
myIndexOfTuples=[('a','b','c'),('d','e','f')]
```

```
print myIndexOfTuples
```

[('a', 'b', 'c'), ('d', 'e', 'f')]

```
print('\n'.join(str(x) for x in myIndexOfTuples))
```

('a', 'b', 'c')
('d', 'e', 'f')
